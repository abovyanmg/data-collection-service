# GitHub Actions для автоматической сборки Docker образов

## 🔧 Настройка Secrets

Для работы автоматической сборки необходимо настроить GitHub Secrets:

1. Перейдите в настройки репозитория: `Settings` → `Secrets and variables` → `Actions`
2. Добавьте следующие secrets:

### DOCKER_HUB_USERNAME
- **Значение:** `abovyanmg`
- **Описание:** Имя пользователя Docker Hub

### DOCKER_HUB_TOKEN
- **Значение:** Пароль от Docker Hub или Access Token
- **Описание:** Токен для авторизации в Docker Hub
- **Как получить:**
  1. Войдите в Docker Hub
  2. Перейдите в `Account Settings` → `Security`
  3. Создайте новый Access Token
  4. Скопируйте токен и добавьте в GitHub Secrets

## 🚀 Как это работает

GitHub Actions автоматически:
- Собирает Docker образ при каждом push в `main`
- Публикует образы с тегами: `latest`, `stable`, и версией по коммиту
- Использует кэш для ускорения сборки

## 📝 Триггеры

Автоматическая сборка запускается при:
- Push в ветку `main` (изменения в `admin/library/acs_data_library/**` или `admin/docker/**`)
- Ручной запуск через `Actions` → `Build and Push Docker Images` → `Run workflow`

## ✅ Проверка работы

После настройки secrets и push в main:
1. Перейдите в `Actions` в репозитории
2. Убедитесь, что workflow запустился
3. Проверьте логи сборки
4. Убедитесь, что образы появились в Docker Hub

