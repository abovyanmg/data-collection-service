# 🔄 Миграция существующих клиентов на версионирование

## ⚠️ Важно для существующих клиентов

**Хорошая новость:** Ваши текущие системы продолжат работать без изменений!

Существующие образы остаются на серверах и будут работать как прежде. Изменения касаются только новых установок и обновлений.

## 🎯 Зачем нужна миграция?

1. **Безопасность отката** - возможность быстро вернуться к рабочей версии при ошибках
2. **Стабильность** - использование проверенных версий вместо latest
3. **Контроль обновлений** - выбор времени обновления

## 📋 Варианты миграции

### Вариант 1: Минимальная миграция (рекомендуется)

Просто обновите `docker-compose.yml` для использования стабильных версий:

```bash
# 1. Остановите текущие контейнеры
docker-compose down

# 2. Скачайте обновленный docker-compose.yml
wget https://raw.githubusercontent.com/abovyanmg/data-collection-service/main/client/docker-compose.yml -O docker-compose.yml.new

# 3. Проверьте изменения
diff docker-compose.yml docker-compose.yml.new

# 4. Если все ОК, замените файл
mv docker-compose.yml.new docker-compose.yml

# 5. Запустите с новыми версиями
docker-compose pull
docker-compose up -d
```

### Вариант 2: Полная миграция на конкретную версию

Для максимальной стабильности зафиксируйте конкретную версию:

```yaml
# В docker-compose.yml измените:
services:
  upload_data_example:
    image: abovyanmg/acs-upload-data:v1.0.0  # Конкретная версия
```

### Вариант 3: Оставить как есть

Если система работает стабильно, можно ничего не менять:

```yaml
# Продолжить использовать latest
services:
  upload_data_example:
    image: abovyanmg/acs-upload-data:latest
```

⚠️ **Недостаток:** При обновлении может скачаться нестабильная версия.

## 🔍 Проверка текущего состояния

Проверьте, какие образы у вас используются:

```bash
# Посмотреть текущие образы
docker images | grep abovyanmg

# Проверить версию в docker-compose.yml
grep image docker-compose.yml

# Проверить статус контейнеров
docker-compose ps
```

## 📝 Пошаговая инструкция миграции

### Шаг 1: Резервное копирование

```bash
# Создать бэкап текущей конфигурации
cp docker-compose.yml docker-compose.yml.backup

# Экспортировать настройки (если нужно)
docker-compose config > docker-compose.config.backup
```

### Шаг 2: Обновление конфигурации

```bash
# Скачать обновленный docker-compose.yml
wget https://raw.githubusercontent.com/abovyanmg/data-collection-service/main/client/docker-compose.yml

# Или вручную изменить в существующем файле:
# image: abovyanmg/acs-upload-data:latest
# На: image: abovyanmg/acs-upload-data:stable
```

### Шаг 3: Тестирование

```bash
# Проверить синтаксис
docker-compose config

# Запустить с новой конфигурацией
docker-compose pull
docker-compose up -d

# Проверить логи
docker-compose logs -f
```

### Шаг 4: Подтверждение работы

```bash
# Проверить статус
docker-compose ps

# Проверить доступность ClickHouse
curl http://localhost:8123

# Проверить работу сбора данных
docker-compose logs upload_data_example | tail -20
```

## 🔄 Откат при проблемах

Если что-то пошло не так:

```bash
# 1. Остановить контейнеры
docker-compose down

# 2. Восстановить старую конфигурацию
cp docker-compose.yml.backup docker-compose.yml

# 3. Запустить со старой версией
docker-compose up -d
```

## 🆕 Что нового для новых клиентов?

Новые клиенты автоматически получают:

1. **Стабильные версии** по умолчанию (`stable` тег)
2. **Документацию** по версионированию
3. **Руководство** по откату при ошибках

## ❓ FAQ

### Нужно ли мигрировать сейчас?

**Нет, не обязательно.** Существующие системы будут работать. Миграция нужна только для:
- Получения преимуществ версионирования
- Безопасного отката при ошибках
- Использования стабильных версий

### Что произойдет с моими данными?

**Ничего.** Данные хранятся в volumes Docker и не затрагиваются при миграции.

### Можно ли использовать latest?

**Да**, но рекомендуется использовать `stable` для production окружений.

### Как часто обновлять?

- **`stable`** - обновляется при стабильных релизах
- **`latest`** - обновляется при каждом коммите
- **`v1.0.0`** - никогда не обновляется автоматически

## 📞 Поддержка

Если возникли вопросы при миграции:

1. Проверьте логи: `docker-compose logs -f`
2. Создайте Issue: https://github.com/abovyanmg/data-collection-service/issues
3. Напишите на email: abovyan.mg@gmail.com

---

**Создано с ❤️ для автоматизации сбора данных**

