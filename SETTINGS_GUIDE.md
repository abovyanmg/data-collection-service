# 📖 Руководство по заполнению settings.xlsx

## Общая структура файла

Файл `settings.xlsx` содержит несколько листов (вкладок), каждый из которых отвечает за настройку подключения к определенной платформе.

### 🔧 Конфигурация

**settings.xlsx структура:**
- **Лист 1**: Настройки БД и Telegram
- **Лист 2+**: Конфигурация платформ (токены, даты, отчеты)

**Примеры конфигураций:**
- `settings_example_ozon_wb.xlsx` - Ozon + Wildberries
- `settings_example_mskld.xlsx` - МойСклад + Ozon
- `settings_example_yandex.xlsx` - Яндекс.Директ + VK

---

## 📊 Лист `db` - Настройки базы данных

### Структура:

| Столбец | Название поля | Описание | Пример |
|---------|--------------|----------|--------|
| A | `db_num` | **Номер БД** (уникальный идентификатор) | `1` |
| B | `subd` | **СУБД** (тип базы данных) | `clickhouse` |
| C | `host` | **Хост** (имя контейнера или IP) | `my_clickhouse` |
| D | `port` | **Порт** | `8123` |
| E | `username` | **Имя пользователя** БД | `default` |
| F | `password` | **Пароль** пользователя БД | `your_password` |
| G | `database` | **Название базы данных** | `MyCompany` |
| H | `bot_token` | **Токен Telegram бота** для уведомлений | `1234567:AAxxxxxx` |
| I | `chats` | **ID чатов** через запятую | `123456789` |
| J | `message_type` | **Тип уведомлений** | `all` / `key` / `errors` |

### Типы уведомлений:
- **`all`** - все уведомления (каждая операция)
- **`key`** - только ключевые события (завершение отчетов)
- **`errors`** - только ошибки

### Пример заполнения:

```
Row 1: Заголовки (не трогать)
Row 2: Технические имена полей (не трогать)
Row 3: Пример (можно удалить или оставить)
Row 4: 1 | clickhouse | my_clickhouse | 8123 | default | MyPassword123 | MyCompany | YOUR_BOT_TOKEN | YOUR_CHAT_ID | key
```

**⚠️ ВАЖНО:**
- `db_num` должен быть уникальным числом
- `host` при работе в Docker = `my_clickhouse` (имя контейнера)
- `database` = название вашей компании/проекта (будет создана автоматически)
- **НЕ используйте** кириллицу в `database`

---

## 🛒 Лист `wb` - Wildberries

### Структура:

| Столбец | Название поля | Описание | Пример |
|---------|--------------|----------|--------|
| A | `conn_num` | **Номер подключения** (уникальный) | `1` |
| B | `db_num` | **Номер БД** (ссылка на лист `db`) | `1` |
| C | `add_name` | **Суффикс таблицы** (латиница) | `mycompany` |
| D | `start` | **Дата начала сбора** | `2024-01-01` |
| E | `token` | **API токен** Wildberries | `eyJhbGci...` |
| F | `backfill_days` | **Дней для пересбора** | `3` |
| G | `reports` | **Отчеты** через запятую | `stocks,orders,sales` |
| H | `schedule_time` | **Время запуска** (МСК) | `15:08,20:35` |

### Доступные отчеты WB:

- `stocks` - Остатки товаров
- `orders` - Заказы
- `sales` - Продажи
- `realized` - Реализованные товары
- `incomes` - Поступления
- `nmreport` - Отчет по номенклатуре
- `reklama` - Рекламная статистика (требует отдельный токен!)
- `paid_storage` - Платное хранение
- `excise` - Акцизы
- `cards` - Карточки товаров

### Пример заполнения:

```
Row 4: 1 | 1 | mycompany | 2024-01-01 | YOUR_WB_TOKEN | 3 | stocks,orders,sales,nmreport | 00:15
Row 5: 2 | 1 | mycompany | 2024-06-01 | YOUR_WB_ADS_TOKEN | 1 | reklama | 03:55
```

---

## 📦 Лист `ozon` - Ozon (основные отчеты)

### Структура:

| Столбец | Название поля | Описание | Пример |
|---------|--------------|----------|--------|
| A | `conn_num` | **Номер подключения** | `1` |
| B | `db_num` | **Номер БД** | `1` |
| C | `add_name` | **Суффикс таблицы** | `mycompany` |
| D | `start` | **Дата начала сбора** | `2024-01-01` |
| E | `clientid` | **Client ID** (из API Ozon) | `123456` |
| F | `token` | **API ключ** Ozon | `xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx` |
| G | `backfill_days` | **Дней для пересбора** | `3` |
| H | `reports` | **Отчеты** через запятую | `stocks,products,transactions` |
| I | `schedule_time` | **Время запуска** (МСК) | `23:00,11:00` |

### Доступные отчеты Ozon:

- `stocks` - Остатки на складах
- `products` - Товары
- `products_info` - Полная информация о товарах
- `transactions` - Транзакции (финансы)
- `postings_fbo` - Отправления FBO
- `postings_fbs_rep` - Отчет по отправлениям FBS
- `finance_details` - Детализация финансов
- `finance_cashflow` - Движение денежных средств
- `reklama` - Рекламная статистика (требует отдельную строку!)

---

## 📈 Лист `ozonan` - Ozon Analytics

### Структура:

| Столбец | Название поля | Описание | Пример |
|---------|--------------|----------|--------|
| A | `conn_num` | **Номер подключения** | `1` |
| B | `db_num` | **Номер БД** | `1` |
| C | `add_name` | **Суффикс таблицы** | `mycompany` |
| D | `start` | **Дата начала сбора** | `2024-01-01` |
| E | `clientid` | **Client ID** | `123456` |
| F | `token` | **API ключ** (Ozon Analytics) | `xxxxxxxx-xxxx-xxxx` |
| G | `backfill_days` | **Дней для пересбора** | `2` |
| H | `dimensions` | **Параметры** (измерения) | `day,sku` |
| I | `metrics` | **Показатели** (метрики) | `ordered_units,revenue` |
| J | `reports` | **Отчеты** | `analytics` |
| K | `schedule_time` | **Время запуска** | `01:45` |

---

## 🎯 Общие правила заполнения

### 1. **Структура листов:**
- **Строка 1** - Человекочитаемые заголовки (не трогать!)
- **Строка 2** - Технические имена полей (не трогать!)
- **Строка 3** - Пример заполнения (можно оставить или удалить)
- **Строка 4+** - Ваши данные

### 2. **Номера подключений:**
- `db_num` в листе `db` должен быть **уникальным**
- `conn_num` в листах платформ должен быть **уникальным внутри листа**
- В поле `db_num` листов платформ указываем **ссылку** на номер БД из листа `db`

### 3. **Расписание (schedule_time):**
- Формат: **московское время** (МСК, UTC+3)
- Несколько времен через запятую: `15:08,20:35`
- Форматы: `15:08` или `3:55` (можно без ведущего нуля)

### 4. **Даты (start):**
- Формат: `YYYY-MM-DD`
- Пример: `2024-01-01`

---

## 🔧 Типичные конфигурации

### Минимальная конфигурация для Wildberries:

**Лист `db`:**
```
Row 4: 1 | clickhouse | my_clickhouse | 8123 | default | MyPassword | MyStore | bot_token | chat_id | key
```

**Лист `wb`:**
```
Row 4: 1 | 1 | mystore | 2024-01-01 | wb_token | 3 | stocks,orders,sales | 00:15
```

### Минимальная конфигурация для Ozon:

**Лист `db`:**
```
Row 4: 1 | clickhouse | my_clickhouse | 8123 | default | MyPassword | MyStore | bot_token | chat_id | key
```

**Лист `ozon`:**
```
Row 4: 1 | 1 | mystore | 2024-01-01 | client_id | ozon_token | 3 | stocks,products,transactions,postings_fbo | 23:00
```

---

## 🚨 Частые ошибки

### ❌ Ошибка 1: Кириллица в `add_name` или `database`
```
add_name = "моякомпания"  ← ОШИБКА!
database = "МояКомпания"  ← ОШИБКА!
```
**Решение**: Используйте только латиницу: `mycompany`, `MyCompany`

### ❌ Ошибка 2: Пробелы в `add_name`
```
add_name = "my company"  ← ОШИБКА!
```
**Решение**: Используйте подчеркивание: `my_company`

### ❌ Ошибка 3: Неверный формат времени
```
schedule_time = "15:08:00"  ← ОШИБКА!
```
**Решение**: Формат `HH:MM`: `15:08`

---

## 🔑 Получение токенов

### Wildberries:
1. Личный кабинет WB → **Настройки** → **Доступ к API**
2. Создайте новый токен (тип: "Статистика")
3. Скопируйте токен (**показывается один раз!**)

### Ozon:
1. Seller cabinet → **Settings** → **API keys**
2. Create new key → Выберите права (Read access)
3. Скопируйте **Client-Id** и **API-key**

### Telegram:
1. Найдите @BotFather в Telegram
2. `/newbot` → создайте бота
3. Получите токен
4. Узнайте Chat ID:
   ```
   https://api.telegram.org/bot<YOUR_TOKEN>/getUpdates
   ```

---

## 💾 Готовые примеры

См. файлы в папке `admin/examples/`:
- `settings_template.xlsx` - пустой шаблон
- `settings.xlsx` - базовый пример

---

## 📞 Поддержка

- **Email**: abovyan.mg@gmail.com
- **GitHub**: https://github.com/abovyanmg/data-collection-service

---

**Разработано: AbovyansConsultingServices (ACS)**
