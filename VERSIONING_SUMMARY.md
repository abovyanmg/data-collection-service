# ✅ Версионирование Docker образов - Итоги

## 🎯 Что было сделано

### 1. ✅ Версионирование на Docker Hub

Созданы версионные теги для всех образов:
- `abovyanmg/upload_data`: `stable`, `v1.0.0`
- `abovyanmg/clickhouse`: `stable`, `v1.0.0`
- `abovyanmg/acs-upload-data`: `stable`, `v1.0.0`
- `abovyanmg/acs-clickhouse`: `stable`, `v1.0.0`

Все теги успешно опубликованы на Docker Hub.

### 2. ✅ Обновлен GitHub Actions

Файл `.github/workflows/build-and-push.yml` обновлен для автоматического создания версионных тегов при каждом коммите:
- Автоматическое создание тегов `stable` для ветки `main`
- Создание версионных тегов на основе git тегов или даты
- Создание тегов по SHA коммита для отладки
- Сохранение тега `latest` для обратной совместимости

### 3. ✅ Обновлен docker-compose.yml

Файл `client/docker-compose.yml` обновлен для использования стабильных версий по умолчанию:
- Используется тег `stable` вместо `latest`
- Добавлены комментарии с вариантами версионирования
- Сохранена обратная совместимость

### 4. ✅ Создана документация

- **NEW_CLIENT_GUIDE.md** - подробное руководство для новых клиентов
- **MIGRATION_GUIDE.md** - гайд по миграции для существующих клиентов
- **README.md** - обновлен с информацией о версионировании
- Скрипт `admin/scripts/create_version_tags.sh` для создания тегов

## 🚀 Процесс подключения новых клиентов

### Простой способ (рекомендуется):

1. **Скачать конфигурацию:**
   ```bash
   wget https://raw.githubusercontent.com/abovyanmg/data-collection-service/main/client/docker-compose.yml
   ```

2. **Подготовить settings.xlsx:**
   ```bash
   wget https://raw.githubusercontent.com/abovyanmg/data-collection-service/main/admin/examples/settings_template.xlsx -O settings.xlsx
   # Заполнить токены API
   ```

3. **Запустить:**
   ```bash
   docker-compose up -d
   ```

**Что происходит:**
- Скачивается `stable` версия образа (максимальная надежность)
- Автоматически поднимается ClickHouse и контейнер сбора данных
- Система готова к работе

### Варианты версионирования для новых клиентов:

**Вариант 1: Стабильная версия (рекомендуется)**
```yaml
image: abovyanmg/acs-upload-data:stable
```
- ✅ Максимальная надежность
- ✅ Проверенные версии
- ✅ Автоматические обновления при стабильных релизах

**Вариант 2: Последняя версия**
```yaml
image: abovyanmg/acs-upload-data:latest
```
- ⚠️ Может содержать экспериментальные изменения
- ✅ Всегда актуальная версия

**Вариант 3: Конкретная версия**
```yaml
image: abovyanmg/acs-upload-data:v1.0.0
```
- ✅ Максимальная стабильность
- ✅ Никогда не обновится автоматически

## 🔄 Для существующих клиентов

**Важно:** Существующие клиенты продолжат работать без изменений!

Локальные образы остаются на серверах и работают как прежде. Для получения преимуществ версионирования можно:

1. **Оставить как есть** - система продолжит работать
2. **Мигрировать на стабильные версии** - см. MIGRATION_GUIDE.md
3. **Зафиксировать конкретную версию** - для максимальной стабильности

## 📊 Преимущества версионирования

✅ **Безопасный откат** - возможность быстро вернуться к рабочей версии при ошибках  
✅ **Стабильность** - использование проверенных версий вместо latest  
✅ **Контроль обновлений** - выбор времени обновления  
✅ **Отладка** - возможность использовать конкретные версии для воспроизведения багов  
✅ **Документирование** - четкая история изменений  

## 🔖 Доступные теги на Docker Hub

### Для upload_data:
- `latest` - последняя версия
- `stable` - стабильная версия (рекомендуется)
- `v1.0.0` - конкретная версия
- `{sha}` - версия по коммиту (автоматически)

### Для clickhouse:
- `latest` - последняя версия
- `stable` - стабильная версия (рекомендуется)
- `v1.0.0` - конкретная версия
- `{sha}` - версия по коммиту (автоматически)

### Для acs-upload-data:
- `latest` - последняя версия
- `stable` - стабильная версия (рекомендуется)
- `v1.0.0` - конкретная версия
- `{sha}` - версия по коммиту (автоматически)

### Для acs-clickhouse:
- `latest` - последняя версия
- `stable` - стабильная версия (рекомендуется)
- `v1.0.0` - конкретная версия
- `{sha}` - версия по коммиту (автоматически)

## 🎯 Рекомендации

### Для новых клиентов:
- Используйте тег `stable` для максимальной надежности
- Следуйте инструкциям в `NEW_CLIENT_GUIDE.md`

### Для существующих клиентов:
- Система продолжит работать без изменений
- При желании мигрируйте на стабильные версии (см. `MIGRATION_GUIDE.md`)

### Для разработки:
- Используйте `latest` для тестирования новых функций
- Используйте конкретные версии для воспроизведения багов
- Теги создаются автоматически при каждом коммите в main

## 📞 Поддержка

Если возникли вопросы:
- GitHub Issues: https://github.com/abovyanmg/data-collection-service/issues
- Email: abovyan.mg@gmail.com

---

**Создано с ❤️ для автоматизации сбора данных**

